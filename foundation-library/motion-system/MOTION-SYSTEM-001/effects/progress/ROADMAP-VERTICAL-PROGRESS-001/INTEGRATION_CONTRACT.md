# Integration contract

The consumer supplies a timeline container, ordered steps, dots and cards. The engine measures actual DOM geometry and writes only CSS custom properties and state classes. It must recalculate on resize, orientation, load and font readiness. Product styling may change rail, dots and cards; the coordinate and clamp relations remain locked. Integration requires separate authorization.
