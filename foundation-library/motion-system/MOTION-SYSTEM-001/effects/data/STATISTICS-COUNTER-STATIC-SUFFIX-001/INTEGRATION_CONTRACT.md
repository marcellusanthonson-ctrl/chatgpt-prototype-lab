# Integration contract

Required DOM: a numeric node and separate suffix node. The numeric slot reserves the final width. JavaScript may mutate only `numericNode.textContent`. Consumers configure start, end, duration, easing and trigger. The product must validate fonts, responsive geometry, reduced motion and final state. Integration requires a separate authorization.
