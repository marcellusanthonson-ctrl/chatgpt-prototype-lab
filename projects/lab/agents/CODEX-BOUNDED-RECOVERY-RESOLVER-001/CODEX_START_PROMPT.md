MODO: CODEX BOUNDED RECOVERY RESOLVER — AUTHORIZATION 188

Repository: marcellusanthonson-ctrl/chatgpt-prototype-lab
Branch: main
Authorization: AUTHORIZATION_LAB_CODEX_BOUNDED_RECOVERY_RESOLVER_CREATION_AND_PRODUCT_LEADERSHIP_TEST003_EXECUTION_188
Agent manifest: projects/lab/agents/CODEX-BOUNDED-RECOVERY-RESOLVER-001/MANIFEST.json
Brief: projects/lab/briefs/CODEX_BOUNDED_RECOVERY_RESOLVER_PRODUCT_LEADERSHIP_TEST003_188_001.json

Apply VERIFY_LIVE_AT_USE. Read `project-sources/chatgpt/START_HERE.md` and follow its exact order, then read the authorization, agent contract, recovery policy, runbook, current continuity, evidence 187, and calibration-v2 manifest.

Create or use an isolated worktree/branch. Execute the mission continuously.

Do not stop on a recoverable technical problem. Classify it, diagnose its root cause, apply only an authorized reversible correction, revalidate, record the cycle, and continue within the five-cycle budget.

For hashes, compare canonical Git blob bytes at the verified HEAD against expected SHA-256 values, and hash the working-tree bytes separately. A local CRLF, encoding, checkout-filter, or dirty-worktree difference is recoverable and is not canonical corruption.

Never inspect or expose secrets. When interactive authentication or another safe user-only action is required, enter `WAITING_FOR_USER_ACTION_RESUMABLE`, publish exactly one action for Jonathan, preserve authorization 188, and resume the same mission after that action.

Require exact `gpt-5.6-sol`. You may restart the existing Codex installation, refresh the model catalog, verify stable provenance, and use an official stable self-update within the existing installation. Do not use prerelease software or substitute the model.

Once all eligibility gates pass, continue under the same authorization through exactly one smoke request, exactly two independent calibration-v2 scorer invocations, and—only after calibration PASS—Execution 004 / Attempt 003 with 52 fixtures, four arms, and at least 112 outputs. Publish the exact result.

Do not mutate calibration-v2 content or expected digests. Do not perform AWS, Terraform, SSE, Phase 3 audit, promotion, activation, integration, product/runtime, Symphonie, selector, pointer, shadow registry, or aggregate-view reconciliation changes.

Return result first, recovery cycles, root causes, fixes, current agent state, any single human action, eligibility, request counts, calibration metrics, Execution 004 status, outputs, publication, authority state, non-effects, and one next action.
